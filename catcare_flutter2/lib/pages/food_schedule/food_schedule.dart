import 'package:flutter/material.dart';

class FoodSchedule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Food Schedule'),
      ),
      body: Center(
        child: Text('This is the Food Schedule page'),
      ),
    );
  }
}
