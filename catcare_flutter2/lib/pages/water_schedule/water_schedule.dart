import 'package:flutter/material.dart';

class WaterSchedule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Water Schedule'),
      ),
      body: Center(
        child: Text('This is the Water Schedule page'),
      ),
    );
  }
}
