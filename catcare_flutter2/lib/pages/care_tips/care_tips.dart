import 'package:flutter/material.dart';

class CareTips extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Care Tips'),
      ),
      body: Center(
        child: Text('This is the Care Tips page'),
      ),
    );
  }
}
